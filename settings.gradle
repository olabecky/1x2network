pluginManagement {
    plugins {
        id 'org.springframework.boot' version "${springBootVersion}"
    }
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}
rootProject.name = '1x2network'
include 'application'
include 'domain'
include 'service'
include 'web'

